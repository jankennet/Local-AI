fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0
pydantic-settings
sse-starlette
starlette-context
huggingface_hub
requests
# llama-cpp-python is installed separately via setup.sh/setup.bat with
# hardware-specific build flags (e.g. Vulkan for AMD) — not pinned here.
